# pokegre — 작업 전 필독

포켓몬 카드 시세·세트·센터링·일러스트 사이트. React+TS+Vite 프론트 / Node·Express 서버
(`server/api.ts`의 `mountApi`, Node 24로 .ts 직접 실행). Fly.io 배포(app `pokegre`, 도쿄).
**사용자는 비개발자다 — 쉬운 한국어로, 짧게, 습니다체로.**

## 🧭 나는 어느 팀인가 — **여기부터 정한다**

2026-08-18부터 세 팀이다. **내 팀 파일을 이 파일 다음에 읽는다.**

| 팀 | 맡는 것 | 읽을 파일 |
|---|---|---|
| **시세팀** | 시세 검색·도감·덤프·팝수·미개봉·낙찰가 고침·PPT 크레딧 | `.claude/시세.md` |
| **게임팀** | 포켓몬 디펜스 시즌1 | `.claude/게임.md` |
| **홍보팀** | 검색 노출·사이트 문구·이름 번역 | `.claude/홍보.md` |
| (셋 다) | 배포·백업·복구·신고함 | `.claude/운영.md` |
| (셋 다) | 지나간 사연 — 「왜 이렇게 됐지?」 싶을 때 | `.claude/기록.md` |

⚠️ **남의 팀 일은 붙잡기 전에 한 번 확인한다.** 「그건 ○○팀 몫인데 제가 할까요?」

## 🚧 강제와 부탁 — **이 둘은 무게가 다르다**

**강제**(권한 설정이 막는다 · 내가 우회 못 한다): 배포(`fly deploy`) · 시크릿 · `.env` 읽기 · `git push`
**부탁**(문서에만 있다 · 지켜야 하지만 안 지켜질 수 있다): 그 밖 전부

⚠️⚠️ **말로 된 규칙은 강제가 아니다.** 되돌리기 어려운 것은 문서가 아니라
   `.claude/settings.local.json`의 `deny`로 막는다. 문서를 더 세게 쓴다고 강제가 되지 않는다.

## 🤝 세 팀이 부딪히는 자리

⚠️⚠️ **`server/api.ts`는 한 번에 한 팀만 고친다.** 1만 줄에 창구 33개가 든 한 파일이라,
   기능을 새로 붙이면 거의 반드시 여기를 건드린다. 고쳐야 하면 **사장님께 먼저 알린다.**
⚠️ **크롬은 한 벌이다.** 낙찰가 러너가 도는 동안은 다른 팀이 크롬을 쓰지 않는다.
⚠️ 파일이 다르면 동시에 일해도 괜찮다. 부딪히면 조용히 깨지지 않고 **오류가 난다.**

## ✅ 「끝났다」의 기준

| 팀 | 통과해야 끝 |
|---|---|
| 셋 다 | `npm run build` |
| 게임 | + `npx tsx scripts/sim-poke-defense.mts` · 통계 셈을 건드렸으면 `npm run 대전쟁통계검사` |
| 시세 | + 관련 검사 스크립트 · **크레딧을 미리 계산해 승인받기**(`limit × 3 × 요청수`) |
| 홍보 | + 밝은·어두운 화면 **글씨 대비 4.5 이상** |

⚠️ **배포는 「끝」에 안 들어간다.** 사장님이 말할 때만 한다.

## 🕵️ 조용한 실패를 의심한다

오류가 안 나고 그럴듯한 결과가 나오는 것이 제일 무섭다. 2026-08-18 하루에만 세 번 겪었다 —
러너 다리가 끊겼는데 「큐가 비었다」로 읽혀 20분을 헛돌았고, 절대 못 사는 카드가 검사기에서
조용히 건너뛰어졌고, 통계가 안 쌓이는데 오류가 없어 코드를 두 번 고쳤다(원인은 `notrack` 표시).

> **0이 나오면 그게 진짜 0인지 따로 확인한다.** 「없음」과 「못 읽음」은 다르다.
> **숫자가 너무 깔끔하면 의심한다.**
> **안 쌓인다·안 나온다 → 코드보다 스위치부터 본다**(`pokegre_notrack` · `document.hidden` · 권한).
> **점검은 「200이 뜨나」가 아니라 「말이 되나」로 한다** — 어제 데이터가 오늘 화면에 나오나.

## 📝 문서 쓰는 규칙 — **이게 없어서 2,244줄이 됐다**

> 새로 알게 된 것은 **규칙 한두 줄**만 해당 팀 파일에 적는다.
> **사연·회귀 목록·실측 표는 `.claude/기록.md`로** 보내고, 규칙에서는 한 줄로 가리킨다.
> **코드에서 지운 것은 문서에서도 같이 지운다** — 2026-08-18에 죽은 이름 25개가 그렇게 남아 있었다.

---

## ⚠️ 최우선 규칙

**① 추측 금지 — 사실은 검증하고 말할 것.** API 기능·요금제·데이터 범위 같은 사실을 **마케팅 문구나 기억으로 넘겨짚지 말고**, 실제 **문서·API 응답·코드**로 확인한 뒤 답한다. 확인 못 하면 "미확인"이라고 명시(넘겨짚어서 틀린 적 반복됨 — 예: PPT 팝수를 "PSA 위주"라 잘못 말함, 실제론 GemRate 다회사). 검증 소스: PPT 문서 https://www.pokemonpricetracker.com/docs (JS 로딩이라 **브라우저 preview로 열어야** 읽힘), 각 API 직접 호출, 리포 코드.

**② 보유한 기능·API에 맞춰 일할 것.** 어떤 작업을 지시받든, 시작 전에 아래 "보유 API·결제 항목" 표를 먼저 보고 그 자원을 활용해 계획·수행. 맨땅에서 시작하거나 이미 결제·발급된 도구를 잊고 헤매지 말 것.

- 예: "세트 이미지 채워줘" → 무료 pokemontcg 키·유료 PPT(이미지 포함)를 **먼저 떠올려** 그걸로 함.
- 예: "시세 관련" → PPT(유료) 기능을 최대한 활용.
- 새 작업 전 `.env`도 함께 확인(값은 출력 금지, 존재·용도만). 새 API가 생겼는데 표에 없으면 사용자에게 확인 후 추가.

## 보유 API · 결제 항목 (`.env`)

| 키 | 용도 | 결제 | 코드 사용 | 주의점 |
|---|---|---|---|---|
| `POKEMON_PRICE_TRACKER_API_KEY` | **PPT** (pokemonpricetracker.com/api/v2). eBay·TCGplayer 시세, 등급별 히스토리, **팝수** | **유료(Business $99)** | server/api.ts (Bearer) | `/cards` 응답에 **카드 이미지도 포함**(`imageCdnUrl` 200/400/800, tcgplayer-cdn). `setName`으로 조회 가능. **2026-08-07에 Pro($10)에서 올렸다 — 하루 200,000크레딧·분당 500요청**(헤더로 직접 확인). `page` 파라미터 없음(`limit`만, 최대 250). |
| `POKEMONTCG_API_KEY` | **pokemontcg.io** 카드 이미지·데이터 | 무료 | **코드 미사용(데이터 작업용)** | 이미지 채우기 등 스크립트에서 `X-Api-Key` 헤더로. 키 없이 부르면 rate limit 걸림 → **반드시 이 키 사용**. 데이터 API가 가끔 500 뜸. |
| ~~`EBAY_APP_ID` / `EBAY_CERT_ID`~~ | ~~이베이 Browse API — 한글판 호가~~ **2026-08-10에 기능을 뺐다** | 무료 | **코드 미사용** | 뺀 까닭: 나머지는 **낙찰가**인데 이것만 **호가**라 같은 화면에서 값의 뜻이 달랐고, 매물 **제목으로만** 찾아 세트·번호를 못 좁혔다(무작위 10장 중 매물이 있던 4장이 **전부 다른 세트** · 2026-08-07 실측). 키는 Fly 시크릿에 **남아 있다** — 지울지는 사장님이 정한다(`fly secrets unset EBAY_APP_ID EBAY_CERT_ID -a pokegre`). 되살리려면 server/api.ts의 「(없앰) 이베이 한글판」 자리와 App.tsx 판 토글에 되돌린다. |
| `ANTHROPIC_API_KEY` | 카드 사진 스캔(이미지→카드 인식) | 종량제 | server/api.ts | **로그인은 안 건다**(시세 조회를 비로그인도 되게 한 것과 같은 뜻). 대신 횟수로 막는다 — 1인당 시간당 10번(`SCAN_RATE_LIMIT`) + 하루 전체 300번(`SCAN_DAILY_LIMIT`). 두 검사 모두 Anthropic을 부르기 **전에** 걸린다. 모델 `claude-sonnet-5`(홀로 카드 작은 글씨 판독). 받는 형식은 jpeg·png·webp·gif만(HEIC를 그냥 넘기면 요금만 쓰고 실패). |
| `KAKAO_REST_API_KEY` / `KAKAO_CLIENT_SECRET` | 카카오 로그인 | 무료 | server/api.ts | |
| `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET` | 네이버 로그인 | 무료 | server/api.ts | |
| `OPENCHAT_URL` | 커뮤니티 오픈채팅 링크 | — | server/api.ts | |

## 💰 요금제 · 월 비용 (사용자 확인, 2026-07 기준)

| 항목 | 요금제 | 비용 | 메모 |
|---|---|---|---|
| **PPT (PokemonPriceTracker)** | Business | **월 $99** (매달 정기결제) | 시세 + 카드 이미지 + 팝수. 2026-08-07에 Pro($10)에서 올림 |
| **Fly.io** (서버) | 종량제(구독 아님) | **약 월 $6.7** + 트래픽 | shared-cpu-1x **1GB** 1대($5.92 · 2026-08-23에 512MB $3.32에서 올림 — 서버 기본 사용량 305MB로 459MB 기계가 넘쳐 2시간 먹통) + **5GB 볼륨**($0.75), 도쿄(nrt). ⚠️ 볼륨은 **산 크기로** 청구된다(쓴 만큼이 아니다 · $0.15/GB/월 · fly.io/docs/about/pricing 2026-08-22 확인) — 그래서 **그림 저장 천장을 올려도 돈은 안 는다.** 2026-08-21에 1GB→5GB로 늘리며 월 $0.60이 붙었다. 무료체험 2026-07-22 종료, 카드 등록됨. 실제 청구액은 Fly 대시보드. |
| **pokegre.com 도메인** | 연 갱신 | **연 ₩24,000** | **가비아(Gabia)**. 2026-07-17 등록 → 2027-07-17 만기. (pokegre.kr도 이벤트로 보유) |
| pokemontcg.io / 카카오 / 네이버 | 무료 | ₩0 | |
| **Anthropic** (사진 스캔) | 선불 충전 | **$5 충전** (2026-07-23 잔액 $3.82) | platform.claude.com. 스캔할 때만 소량 차감. 잔액은 시점따라 변함. |

**월 고정비 합계 ≈ $106 (약 ₩15만) + 도메인 연 약 ₩2만.** (2026-08-07 PPT Business · 2026-08-21 볼륨 5GB · 2026-08-23 메모리 1GB)
※ 정확한 청구액·잔액은 각 대시보드에서 사용자가 확인(에이전트는 결제 화면·잔액 접근 안 함).

## 🗺️ 이미 구현된 기능 · 데이터 흐름 (⚠️ "없다/안 쓴다" 단정 전에 여기부터 확인)

**어떤 기능을 "추가하자/없다/안 쓴다"고 말하기 전에, 이 지도와 실제 코드부터 확인할 것.** (이미 있는 걸 잊고 새로 만들자 하거나 "안 쓴다"고 잘못 말한 적 있음 — 사용자 반복 지적.)

**화면(App.tsx의 `MainView`):**
- 누구나: `cards`(시세) · `artists`(일러스트레이터) · `centering`(센터링) · `sets`(세트별 목록) · `packsim`(오늘의 상점) · `community` · `mypage`
- 운영자만(`adminOnly`): `reports`(신고함) · `stats`(통계) · `scantest`(스캔 테스트) · `flea`(플리마켓)

⚠️ 공개 범위는 App.tsx의 `adminOnly` 한 줄이 정한다. 여기 적힌 것과 코드가 다르면 코드가 맞다.

**소스 → 기능 매핑 (핵심 — 자주 까먹음):**
| 데이터 소스 | 앱에서 이미 하는 것 |
|---|---|
| **PPT** | **eBay 등급별 시세·시세추이 차트·총 낙찰건수(EbayCardDetail/EbayPriceChart/EbayCompareView)** + **TCGplayer 마켓가(TcgPlayerCardDetail)** + 카드 이미지. 등급회사 PSA·BGS·CGC·SGC·TAG 다 받음. → **등급 시세 기능은 이미 있음!** |
| **SNKRDUNK** | 일본판 실거래가·거래이력(CardTile/CardDetail/PriceChart) |
| **TCGdex** | 세트/카드 DB(public/sets/*.json → SetsView), 일러스트레이터(ArtistsView) |
| **pokemontcg.io** | 세트 카드 이미지 보강 |
| **Anthropic** | 사진→카드 인식(CardScanButton/cardScan.ts) |
| **Kakao/Naver** | 로그인 → 커뮤니티/마이페이지(즐겨찾기·최근본·계정연동) |
| **PPT sealed 덤프** | **「미개봉 시세」 모아보기 화면**(도감▾ 4번째 · `/sealed` · SealedPricesView). 세트별 부스터 박스·팩·ETB를 한 표로 — 판 필터·정렬·검색·세트로 이동. 처음엔 세트 상세마다 한 줄씩 붙였다가 "한곳에 모아 보게" 지시(2026-08-11)로 화면을 뗐다. 서버 `미개봉해석`→`/data/sealed-prices.json`→`/api/local/sealed-prices`(slug 없으면 전체). 종류별 대표는 **파는 사람 많은 것→싼 것** 순, 판(ja/en) 갈라 붙임. 통계 이벤트 `sealed` |
| 부가 | 한국 뉴스(PokemonNews), 인기검색어·자동완성, 환율(KrwHint), 공유버튼 |

⚠️ **이베이 한글판은 2026-08-10에 뺐다.** 판 토글은 이제 **일본판·영문판 둘뿐**이다.
   "한글판 시세가 있다"고 말하지 말 것 — 위 「보유 API」 표에 까닭이 적혀 있다.

**아직 안 쓰는(=보강 여지) PPT 데이터:** 등급별 판매 "개수"·거래 속도(salesVelocity)는 받는데 화면엔 덜 씀.

## 🌙 어두운 화면(다크 모드) (2026-08-14)

목적은 **어두운 곳에서 눈이 덜 아픈 것**이다(사장님 지시). 절전이 아니다.

⚠️⚠️ **색 클래스는 한 줄도 안 고쳤다.** 이 앱에 색 클래스가 1,472곳인데 거의 전부
   `neutral` + 검정/흰색이고, Tailwind가 그 색을 CSS 변수로 들고 있다. **변수 값만
   뒤집으면** 화면 전체가 따라오고 **앞으로 만드는 화면도 저절로** 적용된다.
   → `src/index.css`의 「어두운 화면」 절 하나가 전부다.
   ⚠️ 되는 까닭은 이 리포가 색을 **뜻대로** 써 왔기 때문이다(옅은 바탕=낮은 숫자,
      진한 글씨=높은 숫자). **이 규칙을 깨고 쓰면 다크에서 뒤집힌다.**

**눈에 맞춘 선택 셋**
- **순검정(#000)을 안 쓴다** — 흰 글씨와 대비가 너무 세서 글자가 번져 보인다(halation).
  바탕 `#141414` · 카드 면 `#1a1a1a` · 제일 밝은 글씨도 `#ededed`.
- **목록의 카드 사진만 밝기 88%**(`.card-dim`). 바탕만 어둡게 하면 **밝은 사진이 어두운
  판에 박혀 더 눈부시다** — 이 사이트는 화면의 상당 부분이 카드 사진이다.
  마우스를 올리거나 카드를 열면 100%로 돌린다(카드가 주인공이라 아예 어둡게 두면 안 된다).
- **마켓 색은 안 뒤집고 채도만 낮춘다** — eBay 파랑·TCGplayer 주황은 **뜻이 색 자체**다.

**켜는 곳: 「도구 ▾」 메뉴의 맨 아랫줄**(`src/components/ThemeToggle.tsx`). 화면 통틀어
**여기 하나뿐**이다 — 머리말에도 마이페이지에도 없다. 누르면 뒤집히고 메뉴가 닫힌다.

⚠️⚠️⚠️ **자리를 아홉 번 옮기고 제자리로 돌아왔다**(2026-08-14). 옮기자는 말이 나오면
   `.claude/기록.md`부터 읽을 것. 거기서 나온 규칙 셋은 지금도 그대로 맞는다:

   **① 자리 이야기는 말로 견주면 답이 안 난다 — 그려서 보인다.**
      여덟 번을 글로 견주다 다 퇴짜맞았고, 임시 스위치로 **실제로 그려 보여 드린 뒤에야**
      정해졌다. 비슷한 결정이 오면 설명하지 말고 그린다. (그린 뒤 임시 스위치는 반드시 지운다.)

   **② 떠 있는 단추(FAB)는 이 사이트에서 못 쓴다.** 아래쪽 가장자리에 이미 셋이 깔려 있다 —
      카드 상세 시트 · 비교 담기 바 · 카드 뽑기 하단 바(전부 `fixed bottom-0`, z-40).
      실측으로 그 자리를 찍으니 **단추가 아니라 시세 그래프**가 잡혔다. 덮으면 카드 그림을 가린다.

   **③ 자리가 「있다」는 것은 거기 「있어야 한다」는 이유가 아니다.**
      잘못된 이유로 넣었다가 잘못된 이유로 뺐고, 여덟 자리를 돌아 제자리로 왔다.

⚠️⚠️ **부품은 한 벌이다**(`ThemeToggle`). App.tsx와 MyPage.tsx에 따로 만들었다가 합쳤다 —
   자리를 옮길 때마다 한쪽만 고치게 된다. **같은 것을 켜는 자리도 하나뿐이어야 한다**
   (두 자리면 서로 어긋난다 — 마켓 토글에서 이미 한 번 겪었다).
⚠️ 옆 항목(오늘의 상점·팝수·센터링)과 **글자 크기·여백을 그대로** 맞추고, 성격이 다르니
   **줄을 그어 갈라 놓는다**(위는 화면 이동, 아래는 화면 설정).
⚠️⚠️ **설명 글을 붙이지 말 것**(사장님 지시 2026-08-14: "그냥 저렇게 설명 넣지말고").
   제목+설명 두 줄(「어두운 화면 / 어두운 곳에서 눈이 덜 아픕니다」)로 만들었다가
   퇴짜맞았고, 두 칸짜리 토글(다크모드|화이트모드)도 「하나만 쓰라」고 다시 지시받았다.
   「시스템 따라가기 / 밝게 / 어둡게」 세 갈래도 그전에 뺐다 — **고를 것은 하나다.**
⚠️ 낱말은 사장님이 고른 그대로다(다크모드·화이트모드). 글자는 **「지금」이 아니라
   「누르면 갈 곳」**이다 — 화이트모드로 보고 있으면 「다크모드」라고 적힌다.
⚠️ **`header-brand` 안에는 넣지 마라**(머리말로 되돌릴 일이 생기면). 폰에서 카드 상세
   시트가 열리면 그 덩어리가 접혀(`[data-sheet='open']`), **카드를 열어 둔 동안 단추가
   사라진다.**
⚠️⚠️ **머리말 폭을 잴 때 한 번 틀렸다.** 「메뉴 줄에 남은 자리 24px」이라 보고 판단했는데,
   그건 메뉴 줄 **자체 폭**과 비교한 값이었다. 부모 칸 기준으로 재니 375px에서 40px
   아이콘을 넣어도 **한 줄(332px · 9px 여유)**이었다. **폭은 부모 기준으로 잴 것.**
⚠️ 첫 그림 전에 발라야 번쩍임이 없다 — `index.html`의 인라인 스크립트가 먼저 한 번 바르고
   그 뒤는 `src/lib/theme.ts`가 맡는다. **두 곳의 열쇠 이름(`pokegre_theme`)을 같게 둘 것.**
- 한 번도 안 고른 사람은 **기기 설정을 따라간다**(`prefers-color-scheme`, 바뀌면 같이 바뀜).

**점검 방법 — 글자·바탕 대비를 계산해 훑는다.** 홈·시세·커뮤니티·마이페이지·오늘의 상점·
센터링·팝수·세트·작가·미개봉 **전부 통과**.
⚠️⚠️ **Tailwind v4는 색을 `oklch`로 낸다.** 색 문자열에서 숫자를 뽑아 쓰면 rose-500을
   거의 검정으로 읽어 **헛것을 잡는다**(처음에 3건 잘못 잡았다). **캔버스에 칠해서 실제
   RGB를 읽을 것.**

### ⚠️⚠️⚠️ 카드 그림은 **반드시 `cardImg()`를 거친다**

안 거치면 조용히 **카드 뒷면**이 뜬다. TCGdex 주소는 확장자가 없는 베이스라
(`https://assets.tcgdex.net/ja/SV/SV2a/152`) 그대로 프록시에 넣으면 302가 오고,
화면은 오류 없이 뒷면을 띄운다. `cardImg()`가 `/high.webp`를 붙여 준다.

⚠️⚠️ 2026-08-13에 `CardImg` 부품 자신이 이 함정에 빠져 있었다 — **62개 세트 3,783장**이
   시세 타일·카드 상세·팝수 목록·비교에서 뒷면으로 나갔다.
⚠️ `cardImg()`는 이미 완성된 주소(.png/.jpg/.webp)를 그대로 돌려주므로 **한 번 더 걸어도
   다른 소스는 안 다친다.** 의심되면 그냥 걸 것.
⚠️⚠️ **세트 화면만 보면 못 잡는다.** `SetsView`·`PokedexView`·작가·플리마켓은 각자
   `cardImg`를 거치고 있어 멀쩡했다 — **시세 화면으로 봐야** 보였다. 그림을 점검할 때는
   화면을 여러 갈래로 볼 것.
⚠️ tcgplayer-cdn 그림은 저쪽이 지우면 사라진다 — 2026-08-21 전수 조사로 **1,323장(2.5%)이
   403**이었고 다른 창고(TCGdex·limitless·pokemontcg.io)에도 2장 빼곤 없었다. 죽은 주소는
   **비워 뒀다**(정기 재확인 `check-missing-imgs`가 빈칸을 계속 감시한다). **그날부터 그림
   프록시가 tcgplayer도 받아서 디스크에 저장**하므로(`/api/img`) 한 번 열린 그림은 더는 안 잃는다.
   호스트별 실측 표는 `.claude/기록.md`.
- ⚠️⚠️ **죽은 그림의 마지막 창고는 pokellector다**(2026-08-22). 위 네 곳을 다시 두드려 보니
  tcgplayer는 크기 5종 전부 403(완전히 끝남) · TCGdex는 세트만 있고 그림은 2장 · limitless는
  옛 일본판 세트 자체가 없음 · pokemontcg.io는 XY·SM 트레이너 킷이 없었다. **pokellector에만
  318장이 남아 있었다**(`scripts/fill-imgs-pokellector.mts`).
  ⚠️ **번호와 이름이 둘 다 맞을 때만** 쓴다 — L1 34번이 우리는 코터스, 저쪽은 전룡이었다.
  ⚠️ 무늬 변종(미러 홀로)은 저쪽도 일반판 그림뿐이라 `imgBase: true`를 같이 적어
     화면이 **「일반판 그림」**이라고 밝히게 한다(마스터볼 미러 때와 같은 방식).
  ⚠️ `.thumb.png`가 아니라 **원본 `.png`**를 쓴다 — 옛 세트 thumb은 140×195라 뭉개진다.
- ⚠️⚠️ **limitless 세트 코드는 대문자·하이픈 없는 꼴이다**(2026-08-22). 우리 슬러그대로
  `smG`·`M-P`로 두드려 「없다」고 결론냈는데 실제로는 `SMG`·`MP`였고, 거기서 76장을 찾았다
  (`scripts/fill-imgs-limitless-jp.mts`). **「없다」고 적기 전에 대문자·하이픈 뺀 꼴을 같이
  두드릴 것.** 없는 번호에는 403이 오므로 200이면 있는 것이다(헛것 200은 안 준다 · 실측).
  ⚠️ 일본판은 **낱말이 다르다** — Ultra Ball=ハイパーボール · Rare Candy=ふしぎなアメ.
     로마자로 견주면 다 틀린 것처럼 보인다. 포켓몬은 `pokemonNames.json`(ja↔en)으로 맞추고
     트레이너·에너지는 카드쪽 제목을 뽑아 사람이 본다.
- 2026-08-22 기준 **빈칸 947장**이 남았고 여섯 창고(tcgplayer·TCGdex·limitless·pokemontcg.io·
  pokellector·Bulbagarden) 어디에도 없다. 큰 덩어리는 월드 챔피언십 덱 205 · XY/SM 트레이너
  킷 145 · 2001~2010 일본 구축덱 약 400이다. Bulbagarden은 이 카드들을 **영문판 원본 세트
  파일로** 걸어 두어(예: 홀론 덱 → EXDeltaSpecies78) 번호가 안 맞고 언어도 다르다 — 쓰면 안 된다.

### ⚠️ 화면 안에서 무언가를 「열면」 뒤로가기를 같이 걸어야 한다 (2026-08-13)

주소를 안 바꾸고 화면 안에서만 여는 것(팝수 등급표처럼)은 **뒤로가기가 그 화면에
들어오기 전으로 한 칸에 건너뛴다.** 시세 화면은 카드가 시트(`DetailSheet`)로 열려
그 부품이 방문기록 칸을 쌓아 주는데, 그 처리가 없는 화면은 그냥 튕긴다.
→ 같은 `sheet` 표식을 쓰면 된다.

⚠️⚠️ **「목록에서 고른 것」일 때만 건다.** 링크로 바로 들어온 경우(`?id=`)는 **돌아갈
   목록이 없어서**, 걸어 두면 뒤로가기가 **빈 화면에 사람을 세워 둔다**(만들다 겪었다).
   「← 목록으로」 단추를 결과가 있을 때만 내는 것과 같은 잣대다.

### ⚠️ 커뮤니티 목록 — 좋아요가 넓은 화면에서만 안 보였다 (2026-08-14)

폰 줄(`sm:hidden`)에는 좋아요가 들어 있는데 **넓은 화면 칸에는 빠져 있었다**(작성자·작성일·
조회만). 같은 것을 두 군데서 그리므로 **한쪽만 고치면 이렇게 어긋난다.** 칸과 머리글에 추가했다.
⚠️ 좋아요는 `likeCount`가 아니라 **`likedBy` 배열**로 저장된다(서버가 길이를 세어 보낸다).
   저장 파일에서 `likeCount`를 찾으면 **0건으로 잘못 읽는다** — 실제로는 57글 중 51글에 있다.

## 자주 잊는 함정 (오늘까지 배운 것)

- ⚠️⚠️⚠️ **셸 변수 이름에 한글을 쓰지 마라 — zsh가 통째로 못 알아듣는다.**
  `앞=0` 같은 줄이 `command not found: 앞=0`으로 죽고, 그 뒤 셈이 전부 빈 값으로 흘러
  **틀린 답을 조용히 내놓는다.** 2026-08-18에 **세 번** 밟았다:
  ① 파일 감시 루프가 빈 값과 비교해 **「바뀌었습니다」를 늘 참**으로 냈다
  ② 러너 감시가 exit 1로 죽었다 ③ `stat` 한 줄이 「파일 없음」으로 나왔다.
  → **셸에서는 ASCII 이름만**(`prev`·`done_n`). 한글 이름은 파이썬·TS 안에서만 쓴다.
  ⚠️ 곁들여: 배경에서 기다릴 때 `read -t 5 _ </dev/null`은 **즉시 끝난다**(EOF).
     `sleep`을 쓰거나 Monitor를 쓸 것 — 이것도 같은 날 「6분 기다림」이 0초로 끝나 겪었다.



- ⚠️⚠️ **등급 이름 대소문자 — 영문판 뽑기가 통째로 망가져 있었다**(2026-08-11 발견).
  확률표는 `'Hyper rare'`인데 세트 자료는 `'Hyper Rare'`라, **영문판 15개 세트에서
  상위 등급이 하나도 안 나왔다.** 두 군데가 다 걸렸다: ① `groupByRarity`의 풀 열쇠
  ② `RARITY_RANK`/`usableCards` — 후자는 더 나빠서 **카드 자체를 뽑기에서 뺐다**
  (en-sv10 256장 중 **57장**이 빠져 있었다). 지금은 `등급열쇠()`로 소문자 비교한다.
  ⚠️ 자료가 TCGdex·PPT·limitless에서 오고 표기가 제각각이라 **한쪽을 고치는 걸로는
     또 어긋난다.** 등급으로 뭘 찾을 땐 반드시 소문자로 맞출 것.
  ⚠️ 확률표만 보고 "맞게 돌아간다"고 하면 안 된다 — **실제로 뽑아 세어 봐야** 잡힌다
     (`npm run 뽑기검증`).

- ⚠️⚠️ **새 길을 만들면 옛 길이 하던 것을 빠짐없이 옮겨라.** 해외 시세 검색을 「도감으로 먼저
  찾기」로 바꾸면서 **쪽 넘기기를 빼먹었다.** 도감에 영문판 리자몽이 **240장**(20쪽) 있는데
  화면엔 12장만 나오고 「더 보기」도 안 떴다 — 옛 길에서는 되던 것이다(사장님 지적 2026-08-10:
  "리자몽 영문판을 검색했는데 데이터가 12개만 나오는게 말이 되냐"). **새 길이 더 좋아도, 옛 길이
  하던 일 하나를 잃으면 그건 후퇴다.** 바꾸기 전에 옛 길이 무엇무엇을 했는지 먼저 적어 놓을 것.
- ⚠️⚠️ **heredoc 안의 JS 템플릿 문자열로 정규식을 쓰면 `\b`가 백스페이스 문자로 변한다.**
  `/\bjapan\b/`가 파일에는 `/{백스페이스}japan{백스페이스}/`로 적혀 **아무것도 안 잡는데
  눈에는 안 보인다** — 2026-08-10에 판 가르기 정규식 두 개가 이렇게 죽어 있었다(비슷하게
  `̀`이 `\\u0300`으로 망가진 적도 같은 날 있었다). 정규식이 든 코드는 heredoc 치환
  말고 **Edit 도구로** 고칠 것. 의심되면 제어문자 검사:
  `node -e '...charCodeAt(0)<32 && ch!=="\n" && ch!=="\t"...'`
- ⚠️ **"우리에 없다"고 말하기 전에 세어 볼 것.** 갈라 놓은 조각이 도감 카드와 안 이어지는 것을
  보고 "도감에 없는 카드"라고 말했는데, 실제로는 조각 239장 중 **158장(66%)이 도감에 있었다**
  (리자몽 계열만 387장이 있다). 못 이은 것은 **잇는 규칙이 약해서**지 도감이 비어서가 아니다.
  「내 코드가 못 찾았다」와 「자료에 없다」는 전혀 다른 말이다.

- **세트별 카드 이미지**는 `public/sets/*.json`에 **빌드 시점에 박혀 있다**(런타임에 API 안 부름). 각 카드 `{n, name, img}`. 빈칸(img="") 채우려면 **pokemontcg.io(무료 키) 또는 PPT**로 받아 `--write`. 소스 매칭은 **번호+카드이름 둘 다 일치할 때만**(엉뚱한 이미지 방지 — 사용자 최우선 원칙: "틀린 것보다 빈칸").
- **이미지 프록시** `/api/img`는 wsrv로 축소. **tcgplayer-cdn은 wsrv가 막혀** 302로 원본 폴백(그래도 표시됨). 화이트리스트는 server/api.ts `IMG_ALLOWED_HOSTS`.
- **세트 화면(SetsView)은 공개됐다**(2026-08 기준). "운영자 전용"이라고 적혀 있던 옛 메모를 믿고
  "일반 유저에겐 안 보인다"고 잘못 말한 적 있다. 공개 여부는 App.tsx의 `adminOnly`를 볼 것.
- **환율은 "어제 날짜"가 정상 = 버그 아님.** 유럽중앙은행 일일 발표값(Frankfurter `/latest`)이라 평일 하루 한 번, 보통 전날 값. "7.XX 환율 기준" 라벨은 의도된 표기(실시간 아님을 밝히는 것). FX 수수료로 어차피 2~5% 어긋나서 실시간 살 이유 없음 — 그렇게 설계함. **"환율 안 바뀐다/멈췄다"고 오해 말 것.**
- **트레이너 이름 번역**: 예전 세션이 일본어 음역을 잘못 넣은 게 많았음 → [[pokegre-trainer-romaji-errors]] 참고. 나무위키/Fandom은 WebFetch가 402/403 → **인app 브라우저로 읽기**.
- 배포: `fly deploy -a pokegre` (사용자 인가 하에). 프로덕션 `/data` 읽기는 집계만, PII 금지. **배포 전 검증은 `npm run build`로** — `npx tsc --noEmit`은 통과해도 빌드용 `tsc -b`(프로젝트참조·incremental)가 더 엄격해 다른 에러를 잡는다(한 번 배포 실패함). 새 서버 키는 `.env`뿐 아니라 `fly secrets set ... -a pokegre`도 필요(프로덕션은 .env 안 읽음).

