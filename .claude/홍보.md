# pokegre — 홍보팀

⚠️ **`CLAUDE.md`를 먼저 읽고 이 파일을 본다.** 여기에는 검색 노출·문구·이름 번역·사이트 모양과 배치에 해당하는 것만 있다.
⚠️ 지나간 사연은 `.claude/기록.md`에 있다 — 「왜 이렇게 됐지?」 싶을 때 거기부터 본다.

---

## 사이트 모양과 배치 (2026-08-18부터 홍보팀 몫)

전에는 각 팀이 자기 화면만 봐서 사이트가 제각각이었다. **화면 전반의 모양·배치·통일감은
홍보팀이 맡는다.** 문구·색 대비를 이미 홍보팀이 보고 있어 결이 같다.

⚠️⚠️ **자리·모양 이야기는 말로 견주지 말고 그려서 사장님께 보인다.** 다크모드 스위치 자리를
   여덟 번 글로 견주다 전부 퇴짜맞고, 실제로 그려 보인 뒤에야 정해졌다(`.claude/기록.md`).
   **그린 뒤 임시 스위치는 반드시 지운다.**
⚠️⚠️ **다른 팀이 지금 고치는 화면은 건드리지 않는다.** 파일이 겹치면 조용히 깨진다.
   손대기 전에 지휘부에 「어느 화면을 볼지」 알리고 순번을 받는다.
⚠️ 큰 배치 변경은 **사장님 승인 뒤에** 코드로 옮긴다.
⚠️ 색 클래스는 뜻대로 쓴다(옅은 바탕=낮은 숫자, 진한 글씨=높은 숫자). 이 규칙을 깨면
   어두운 화면에서 뒤집힌다 — CLAUDE.md 「어두운 화면」 절 참고.
- 끝의 기준은 `npm run build` + **밝은·어두운 화면 글씨 대비 4.5 이상**.

## 검색 노출 (이미 등록돼 있음 — 다시 등록하지 말 것)

| | 방식 | 확인법 |
|---|---|---|
| **구글 서치콘솔** | **DNS TXT 기록**(도메인 속성). index.html에 메타 태그 없음 | `dig +short TXT pokegre.com` |
| **네이버** | index.html 메타 태그 `naver-site-verification` | |
| **사이트맵** | `https://pokegre.com/sitemap.xml` — 2026-07-17 제출, 성공 | 서치콘솔 → Sitemaps |

⚠️ 2026-08-01에 "구글에 등록 안 돼 있다"고 잘못 말했다. 메타 태그만 보고 DNS를 안 봤다.
**등록 방식이 여러 가지라 한 곳만 보면 틀린다.**

- 사이트맵은 `node scripts/gen-sitemap.mjs`로 다시 만든다. **2026-08-14 기준 1,124개 제출 ·
  977개 색인(87%)**. (개수는 세트·작가가 늘면 같이 는다. 표준 상한 5만 개라 여유가 많다.)
- 크롤러가 빈 페이지를 보지 않도록, `server/index.ts`가 주소마다 제목·본문을 **글자로 미리**
  넣어 보낸다(`<div id="seo-fallback">`). 앱이 뜨면 App.tsx가 그 조각을 지운다.
  ⚠️ 시리즈 슬러그 규칙은 `src/lib/setNameKo.ts`의 `serieSlug`와 gen-sitemap.mjs가 **같아야** 한다.
     한쪽만 고치면 사이트맵에 적힌 주소가 404가 된다.

### ⚠️⚠️ 안쪽 링크 — 새 쪽을 만들면 **반드시 잇는다** (2026-08-18)

구글은 링크를 따라다니며 쪽을 찾고 **중요도를 매긴다.** 사이트맵은 주소만 알려 줄 뿐이다.
2026-08-18에 재 보니 **안쪽 링크가 0개**였고, 977쪽이 서로 상관없는 낱장이었다.

- `server/index.ts`의 `링크묶음()` · `꼬리링크()`를 쓴다. 꼬리는 **`seoPage()`를 쓰는 쪽에
  자동으로** 붙는다. `seoPage`를 안 쓰고 직접 HTML을 만드는 쪽(`/centering`·`/sealed`·
  `/population`)은 **손으로 붙여야 한다** — 실제로 이 셋만 0으로 남아 있었다.
- 세트 색인은 `세트색인읽기()`로 읽는다(10분 캐시). 작가 json은 카드마다 `s`가 세트 슬러그다.
- ⚠️ **자기 자신을 가리키는 링크는 값이 없다.** 세트 쪽에 그런 링크가 하나 있었다.

### ⚠️ 사람들은 시세를 「○○ 힛카드」로 찾는다 (2026-08-18)

검색어 73개 중 「가격」이 든 것은 하나, 「시세」·「얼마」는 0개인데 **「힛카드」는 열 개가 넘는다.**
`/set/:slug`가 이미 그 말을 받고 있고 순위는 8~36위다. **기준점 표는 `.claude/기록.md`.**

⚠️⚠️ **다음에 잴 때는 순위보다 「노출이 늘었나」를 먼저 본다.** 지금 노출이 1~5회뿐이라
   「평균 순위」는 표본이 한둘이고 우연에 가깝다. 노출이 먼저 늘고 순위가 따라온다.
⚠️ 값이 얇아서 밀리는 게 아니다 — 8위인 세트도 29위인 세트도 **값이 100% 차 있다**(시세팀 확인).

⚠️⚠️⚠️ **새 주소에 seo-fallback을 넣으면 App.tsx가 지우는지 눈으로 볼 것.**
   지우는 `useEffect`에 조건이 걸려 있었고(`viewFromPath`), 홈(`/`)은 늘 빈 값이라
   **홈에서만 크롤러용 글자가 화면에 그대로 남았다.** 앱은 멀쩡히 떠 있어서 오류도 없었다.
   지금은 조건을 걷어냈다. 자세한 실측은 `.claude/기록.md`.
- 사이트맵 내용을 바꾸면 서치콘솔에서 같은 주소로 한 번 재제출하면 빨리 읽어간다.
- 주소가 다 살아 있는지 확인: `<loc>`를 뽑아 전부 눌러 본다(2026-08-02 전수 확인 결과 404 0개).

## 세트명은 공식 한글판 이름이 잣대다 (2026-08-21 · 시세팀이 사장님 지시로 고침)

일본판 세트명 39건이 공식 발매명과 달랐다(옻칠의 가이스트→**칠흑의 가이스트** ·
초폭임팩트→**버스트임팩트** · 방적의 링→**밴디트링** 등). 원천은
`src/data/setNamesKo.json`·`setNameKoPpt.json` 두 표 — **고칠 땐 둘 다** + 색인 재생성.
잣대: 나무위키 「틀:포켓몬 카드 게임 정식발매 확장팩 일람/<시리즈>」(인앱 브라우저로 열고
「펼치기」를 눌러야 보인다). 북미판은 음역(팀 로켓 리턴즈), 한국 미발매 시대(네오·1세대·
e카드·LEGEND)와 **DP는 지금 번역 유지**(한국판 DP는 세트 묶음이 달라 1:1 불가 — 사장님 결정).

## 카드명 번역 점검
**공식 한글 카드명 출처(제일 정확 — 추측하지 말 것):** 포켓몬코리아 카드검색 `https://pokemoncard.co.kr/cards/detail/<CardNum>` 은 서버렌더라 curl로 읽힌다. 이름은 `class="card-hp title"`, 번호는 `class="p_num"`, 이미지 경로에 세트코드가 들어 있다. **CardNum = 세트별 접두사 + 카드번호 3자리**(예: `BS2019007`+`092` = SM10 92번). 접두사는 `BS<연도><세트순번3자리>`라 `...001`을 훑어 세트별로 알아낼 수 있다(2018~2026 매핑을 이미 한 번 떴다). ⚠️ **한국판은 서포트·굿즈 블록을 한글 가나다순으로 다시 매긴다** — 번호가 일본판과 1:1이 아니다. 블록(예: SM12a 148~163) 전체를 받아, 이미 맞는 이름들을 짝지어 두고 **남은 것끼리 소거법**으로 맞춰야 한다. 한국 미발매(1996~2006 옛 세트)는 이 방법이 안 되니 표준 외래어 표기로.

`npx tsx scripts/card-name-audit.mts --list` — ①일본어 잔여 ②음역으로 깨진 이름 ③**공식 카드명 대조**
④**히라가나가 뜻 없이 소리로 남은 것**(`おねがい`가 "부탁"이 아니라 "오네가이"로 나가던 것) ⑤**짧은 별칭이
멀쩡한 카드명에 끼어드는 것**(`ベリー`(캐이시) 때문에 `シトラスベリー`가 "시토라스캐이시"가 되던 것).
④⑤는 2026-07-27에 추가했다 — ①②③만으로는 이 두 종류가 전혀 안 잡혔다.
⚠️ **공식 한글명은 내가 뒤지지 말고 사용자에게 목록으로 보낸다**(일본어 원문·지금 나가는 한글·세트·번호 표).
포켓몬코리아를 한 장씩 훑는 건 너무 느리다 — 사용자가 몇 초면 알려준다.
③은 `scripts/ko-official-card-names.json`(공식 사이트에서 받아 둔 세트별 정식 한글명)이 있어야 도는데,
참고자료라 레포에서 뺐기 때문에 **지금은 건너뛴다**(일치 0/불일치 0으로 표시된다). 다시 받으려면 위 방법으로.
**고치기 전후로 전체 카드명 렌더 결과를 떠서 diff할 것** — 2글자 규칙이 다른 이름을 깨뜨렸는지(예: `デンジ`가 `デンジャラス`를 깸) 이걸로만 잡힌다. 옛 세트(e시리즈·PCG·neo)는 **원본 TCGdex의 일본어 칸이 오염**(정식 일본명 대신 영어명 가타카나 음차: `デンリュウ`가 아니라 `アンファロス`)돼 있어 사전이 못 잡는 게 원인 — 우리 번역기 버그가 아니다. 포켓몬은 `src/data/pokemonNameAliases.json`, 굿즈·트레이너는 `STRUCTURAL_TERMS`에 추가. **별칭은 3글자 이상만**(2글자는 다른 이름에 끼어듦), 추가 전 트레이너 이름과 충돌 검사, 한글명은 `pokemonNames.json`에서 가져올 것. 상세는 메모리 [[pokegre-translation-batch-check]].

### 세트 이름 2차 훑기 — 옛 세트까지 (2026-08-14)

검색어 **45개**로 세트 이름 **176가지**를 모아 다시 훑었다. 1차(33개·136가지)에서 놓친
옛 세트·콜라보 굿즈를 잡으려는 것이었다.

⚠️⚠️ **찾는 방법을 바꿨다 — 이게 핵심이다.** 눈으로 고르면 놓치고 헛것도 잡는다.
   **가타카나 토막마다** 「사전이 걸렸나, 음역으로 샜나」를 기계로 판정한다:
   토막을 `kanaToHangul`에 태운 결과가 **최종 한글에 그대로 남아 있으면 샌 것**이다.
   ⚠️ 「사전이 한 줄도 안 걸렸나」만 보면 **부분만 걸린 것을 놓친다** — 「構築スタンダードデッキ」는
      構築·デッキ가 걸려서 순수 음역과 달랐지만 가운데 `スタンダード`가 「스탄다도」로 새 있었다.

| 일본 이름 | 전 | 후(사장님 확정) |
|---|---|---|
| `構築スタンダードデッキ` | 구축스탄다도덱 | **구축 스탠다드 덱** |
| `クラフトボスカフェ` | 쿠라후토보스카페 | **크래프트 보스 카페** |
| `ミニマム★パック` | 미니마무★팩 | **미니멈 팩** |
| `ドラゴンディスカバリー` | 도라곤디스카바리 | **드래곤 디스커버리** |
| `割るだけ` | 쪼개는다케 | **타먹는 전용** (산토리 농축 음료 표기) |

⚠️ **`ダブルクライシス`는 제목 꼴에 따라 갈렸다.** 띄어쓰기 꼴(`コンセプトパック マグマ団…`)은
   사전에 있어 멀쩡한데 **낫표 꼴**(`コンセプトパック「マグマ団…」`)은 안 걸려 「더블쿠라이시스」로
   나갔다. → **세트 이름은 짧은 알맹이도 같이 넣는다**(`ダブルクライシス` 단독 줄 추가).
⚠️ `割る`(쪼개는)가 아래에 있어 `割るだけ`가 깨졌다 — **복합어 먼저**, 또 그 규칙이었다.

**회귀(고친 뒤 119가지 다시 훑음):** 일본어 남은 것 **0** · 음역이 그대로 남은 토막 **10**,
그런데 **10개 전부 그게 정답**이다(루기아·디아루가·닌텐도·기라티나·코로코로이치반·노부나가·
샤이니스타·루카리오·마나피·코라이돈). 고치기 전에는 같은 잣대로 **17개**가 걸렸고 그중 5개가
진짜 깨진 것이었다.
⚠️ **음역이 정답인 이름이 많다** — 「음역이면 틀린 것」으로 자동 처리하지 말 것. 목록을 뽑아
   **사장님께 여쭙는 것**이 이 작업의 정해진 절차다.

### ⚠️ 세트 이름 — 깨진 넷을 공식 한글명으로 (2026-08-13)

신고함에서 시작해 매물 제목의 세트 이름 **136가지**를 훑어 깨진 것을 골라내고, **사장님이
공식 한글명을 확인해 주셔서** 사전에 넣었다.

| 일본 세트 이름 | 전 | 후(공식) |
|---|---|---|
| `バーニングシャドウズ` | 바닌구샤도우즈 | **버닝섀도우** (SM3+) |
| `エメラルドブレイク` | 에메랄드부레이쿠 | **에메랄드 브레이크** (XY6) |
| `チームアップ` | 치무아푸 | **태그볼트** (SM9) |
| `イントロパック` | 인토로팩 | **인트로 팩** |

⚠️⚠️ **`チームアップ`는 영문명과 한글명이 다르다.** Team Up(영문판)의 일판·한판 이름은
   **「태그볼트」**다. 소리대로 옮기면 영영 못 맞춘다 — **이런 건 물어봐야 안다.**

⚠️⚠️ **`エメラルドブレイク`는 사전에 이미 있었는데도 안 먹었다.** `['エメラルド','에메랄드']`가
   **더 앞줄에 있어서** 앞부분만 먹고 남은 `ブレイク`가 음역됐다. 이 표는 **위에서부터 차례로**
   도므로 **자리가 곧 규칙**이다 — 파일 주석에 「복합어는 조각보다 먼저」라고 적혀 있는데
   그 자리가 어겨져 있었다. 복합어를 조각 앞으로 옮기고, 아래 중복 줄은 지웠다.
   → **새 낱말은 STRUCTURAL_TERMS 맨 앞 구역에 넣는다**(`パック`·`エメラルド` 같은 조각보다 먼저).

⚠️ **낱말만 떼어 시험하면 잘못 잡는다.** 처음에 6개를 깨진 것으로 골랐는데, 실제 제목 꼴로
   다시 재 보니 `ダブルクライシス`·`タッグオールスターズ` 둘은 **긴 이름으로 이미 사전에 있어
   멀쩡했다**(「コンセプトパック マグマ団VSアクア団 ダブルクライシス」·「TAG TEAM GX タッグオールスターズ」).
   **반드시 매물 제목 그대로 넣어 재 볼 것.**

회귀: 세트 이름 **94가지 다시 훑어 깨진 것 0** · 태그볼트·더블 크라이시스·태그올스타즈·
에메랄드·스톰에메랄다·확장팩 모두 그대로.


## 한글 표기 점검 — 2026-08-21 (사장님: 「심큥컬렉션」 지적)
- **도감 카드명**은 `npx tsx scripts/card-name-audit.mts`가 0건이면 끝. **세트 이름**은 `src/data/setNamesKo.json`
  (밴딩머신→자판기·하이크래스→하이클래스 고침). **스니커덩크 제목**은 실시간 변환이라 자료 파일에 없다 —
  `pokegre.com/api/snkrdunk/v3/search?keyword=…`로 인기 검색어 25개를 받아 koreanizeTitle에 넣고 전부 눈으로
  훑는 방식(스크래치 rec/../snk_audit.mts). 사전 고친 뒤엔 **전·후 diff**로 의도한 줄만 바뀌었는지 본다.
- 고친 것: ポケキュン→포켓심쿵 · プライド→프라이드 · バンドル→번들 · レッド→레드 · クリスタルタイプ→크리스탈타입 ·
  シュラウデッドフェイブル→쉬라우디드 페이블 · 表記なし→표기 없음 · 漫才ごっこ→만담 놀이 · CP5 이름 통일.
- 구뒷면 리자몽 변종은 「화염 리자몽」「괴력 리자몽」(사장님 결정). ⚠️ 열쇠는 `かえん리자몽`처럼 **한글 섞인 꼴** —
  포켓몬 이름이 이 사전보다 먼저 바뀌어서 `かえんリザードン`으로는 영원히 안 맞는다.
- 2차(검색어 100개·제목 1,029개): 「소리로만 옮긴 낱말」 자동 추출(원문 가나 덩어리의 음역이 변환문에 그대로
  있으면 사전이 못 잡은 것 — rec/../snk_audit2.mts). 82종 중 진짜 오류 25종을 배열 **맨 앞**에 통째 매핑
  (셀러브레이션·드래곤 셀렉션·소생하는 전설·은하의 정복·갤럭시단·진주단·금강단·삐쭉귀 피츄·렌티큘러…).
  ⚠️ 'オーキド'→'오박사'는 금지 — 'はかせ'가 붙어 「오박사박사」(회귀로 잡음).
- ⚠️ 회귀는 **같은 원문을 고정**해 두고 잰다(rec/../snk_regress.mts). 다시 받아 와서 비교하면 검색 결과가
  매번 달라 22가 53으로 튀어 헛것을 본다.

## 게시판을 「글을 쌓는 곳」으로 — 2026-08-23 (사장님 지시)
- **글마다 주소가 생겼다**(`/community/<번호>`). 그전에는 `/community` 한 쪽만 검색에 올라가서
  정보글을 아무리 써도 **검색 유입이 0**이었다. 서버가 글마다 제목·설명문·본문을 그려 주고
  사이트맵에도 얹는다(정적 sitemap.xml에 서버가 글 주소를 덧붙여 내보낸다).
  ⚠️ 비밀글·가려진 글은 뺀다. ⚠️ App.tsx의 주소 동기화에서 **깊은 링크로 빼 둬야** 한다 —
  안 그러면 글을 여는 순간 주소가 `/community`로 되돌아간다(만들면서 실제로 겪음).
- **글 꾸미기**(`src/lib/postFormat.tsx`). `## 제목` · `**굵게**` · `- 목록` · `| 표 |` · `---` ·
  `[사진1]`/`[사진1 작게]`. HTML을 받지 않고 **우리가 읽어 리액트 조각으로** 바꾼다(누구나 쓰는 글이라).
  사진을 올리면 본문 끝에 `[사진N]`이 자동으로 붙고, 그 표시를 옮기면 자리가 바뀐다.
  ⚠️ 본문이 자리를 잡은 사진은 **아래 사진 묶음에서 뺀다**(안 빼면 같은 사진이 두 번 나온다).
  ⚠️ 꾸미기를 안 쓴 회원 글은 예전과 똑같이 나온다.

## 목록 타일 규칙 — 2026-08-21 (사장님 지적으로 고침)
- **상세가 열리고 닫혀도 목록은 같은 부모 안에 둔다.** App.tsx의 목록+상세 틀을 상세 유무로
  갈아 끼우면 리액트가 목록을 새로 만들어 폰에서 시트를 닫을 때 이미지가 깜빡인다.
  (실측 방법: 닫기 전 첫 img에 표식 → 닫은 뒤 남아 있나. 스크래치 rec/flicker.mjs)
- **타일은 `flex flex-col` + 이름 `min-h-[2.5rem]`·세트 `min-h-[1rem]` + 「시세 없음」 자리.**
  <button>은 내용을 세로 가운데에 두므로 짧은 칸의 그림이 내려앉는다. CardTile·EbayCardTile 둘 다
  이 규칙이다 — 새 타일을 만들면 같이 맞출 것.

## 의견함 배너 — 2026-08-21
- 홈 공지 배너(디펜스 안내)를 내리고 **의견함**(FeedbackBanner)으로 바꿈(사장님 지시).
  한두 문장 의견 → 운영자만 봄(운영 ▾ 신고함 맨 위 「의견함」). 비로그인도 보냄(IP당 시간당 5건).
  보낸 사람·닫은 사람에게는 다시 안 뜸(SENT_KEY/DISMISS_KEY — 다시 띄우려면 날짜 바꾸기).
  통계 trackEvent('feedback')+서버 허용목록+통계 줄까지 같이 넣음. ⚠️ server/api.ts 만짐(/api/local/feedback).
  공지를 되살릴 때는 App.tsx의 그 자리 주석 참고(배너는 한 자리에 하나만).

## 플리마켓 마켓형 개편 — 2026-08-21 (사장님 피드백 대기)
- FleaListings 상단을 세 탭으로: **매물(피드·기본)** / 카드로 찾기(기존) / 내 거래.
  피드는 실물사진 타일 + 판·등급·정렬 거르개, 팔린 것은 흐리게 뒤로. 서버는 안 건드렸다.
- FleaAdmin 래퍼 탭은 「마켓|설정」(안쪽 「매물」과 이름 겹침 피함).
- 로컬 미리보기: 서버가 비로그인에 404라 **플레이라이트 route로 flea API를 가짜로 채워** 찍는다
  (config는 {config,counts} 꼴). 견본 자료는 data/flea-listings.json.
- 2차(2026-08-21 밤): **사진 10칸 고정**(앞·뒤 전체+모서리 4×2 — PHOTO_SLOTS 차례가 곧 자리) ·
  **A~D 기준 카드형 선택**(RAW_GRADE_TITLE/HINT, 상세에도 같은 글) · **감정 회사 드롭다운**
  (SLAB_COMPANIES → "PSA 10" 꼴 저장, isSlab는 「A~D 아니면 감정품」) ·
  **구매 희망(매수) 호가** — 서버 /flea/bids 신설(걸기·내리기·카드별 조회, 카드당 1인 1건 갱신),
  카드 페이지가 매수 빨강·매도 파랑 호가판. 「이 값에 팔기」는 올리기 폼에 값만 미리 채운다
  (자동 맺기는 다음 단계 후보). ⚠️ server/api.ts 만짐(bids 창구·사진 10장 검증·등급 목록).
- 3차(2026-08-21 낮): **거래 대화방**. 구매 희망 「판매하기」·매물 「판매자와 대화하기」로
  1:1 방(카드+참고가를 위에 고정). 글·사진 자유, **최종 금액만 「거래 확정」 버튼**으로 —
  수락되면 FleaDeal(grade 「대화 성사」)로 남고 희망은 내려가고 매물은 팔림 처리.
  새 글은 5초 폴링 + 내 거래 탭 안읽음 배지 — **푸시 알림 없음**(사이트에 들어와야 보인다).
  서버 /flea/chats 창구(방·글·확정, reads로 안읽음 셈). ⚠️ server/api.ts 또 만짐.
  ⚠️⚠️ **지금은 자기 자신과의 방을 일부러 허용**한다(운영자 혼자 프로덕션에서 흐름을
     눌러 보라고 — 제안을 자기가 수락하는 것까지 열어 둠). **회원 공개 전에 반드시 막을 것**
     — `grep -n '회원 공개 전' server/api.ts src/components/FleaListings.tsx` 하면 네 자리가 나온다.
  ⚠️ 공개 때 같이: trackEvent(플리마켓은 운영자 전용이라 아직 통계 없음) + 자기-방 금지.
  로컬 실검사 방법(가짜 route 없이): 스크래치에 users/sessions.json 만들고
  `POKEGRE_DATA_DIR=<스크래치> ADMIN_KAKAO_IDS=9001,9002 PORT=8788 node server/index.ts`
  → 쿠키 pokegre_session=tok-…으로 두 사람 흉내. ⚠️ 8788을 옛 서버가 잡고 있으면
  새 서버가 조용히 죽고 옛것이 응답한다 — lsof로 먼저 확인.
