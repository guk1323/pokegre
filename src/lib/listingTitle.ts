/**
 * 이베이 **매물 제목**에서 사실을 뽑아내는 한 벌. **서버와 점검 도구가 이것만 쓴다.**
 *
 * ⚠️⚠️ 규칙이 두 벌이면 점검 도구가 없는 문제를 만들어 낸다. 2026-08-08에 그 일을
 *    세 번 겪었다 — "Team 로켓단 Grunt 154장"(없는 문제) · 멀쩡한 낙찰 386건 ·
 *    BGS 부점수 "9/9/9.5"를 카드 번호로 읽은 것. 전부 도구가 서버와 다른 길을 써서였다.
 * ⚠️ 서버가 이 파일을 쓰므로 **Dockerfile COPY 목록에도 있어야 한다.**
 *    `npm run build`의 배포 파일 검사가 빠뜨리면 알려 준다.
 */

// ⚠️ 이 말들 바로 뒤 숫자는 **등급 점수**지 카드 번호가 아니다.
//    "CGC PERFECT 10/10"의 10/10을 카드 번호로 읽어 묶음 판매로 오해한 적이 있다(2026-08-08).
export const 등급앞말 =
  /(psa|bgs|cgc|sgc|tag|ace|ars|grade|gem|mint|pgs|mpg|perfect|pristine|black label|subgrade[ds]?|subs?)\s*$/i
/**
 * **제목에 적힌 감정 등급을 "칸 이름"으로 바꾼다**("PSA 10" → psa10 · "CGC 9.5" → cgc9_5).
 *
 * 왜 필요한가 — 저쪽이 제목에서 등급을 못 읽으면 그 매물을 **미감정 칸에 떨어뜨린다.**
 * 사장님 지적(2026-08-08): "사진 보면 싱글카드가 아니라 등급 카드인데, 이름에 psa10이라
 * 안 쓰여 있으니까 싱글카드로 들어 있는 게 있다."
 * 잘 알려진 회사(PSA·BGS·CGC…)뿐 아니라 작은 회사도 잡는다 — 실제로 이런 것이 섞여 있다:
 *     "PGS 10" · "MPG 10" · "Pokemon-Z gold 10" · "CCI 10"
 * ⚠️ 작은 회사를 PSA 칸에 넣으면 **그게 또 섞임이다.** 회사 이름을 그대로 살려
 *    자기 칸(pgs10 · mpg10)을 만든다. 화면의 등급 이름표는 아무 칸이나 받는다.
 */
// ⚠️ **낯선 회사도 이름을 살려 제 칸에 세운다.** 미감정에 섞으면 방문자가 「이건 PSA가
//    아니구나」를 알 길이 없다. `pcg`·`csg`는 2026-08-19에 실물 매물에서 보고 넣었다
//    (「PCG 10」이 미감정 칸에 들어가 있는 것을 사장님이 눈으로 찾으셨다).
/** 등급별로 칸을 따로 세우는 **큰 회사 넷**. 나머지는 「기타 감정 회사」로 모은다. */
const 큰회사 = new Set(['psa', 'cgc', 'bgs', 'brg'])
export const 회사말 = 'psa|bgs|bccg|beckett|brg|cgc|sgc|ace|tag|ars|rgr|pgs|pcg|mpg|cci|csg|gma|hga|isa|ags|pyxis|acg|tqg|aph|lcc|plg|mnt|ccg|mgc|hgc|bctc|cga|dsg|rpa|ksa|jbh|toc|tga|pg|ark|wag|kvu|rcg|gcg|gbtc'
// ⚠️ 회사와 숫자 사이에 등급말이 끼는 꼴이 흔하다 — "PSA NM-MT 8" · "CGC NM/MINT 8" ·
//    "BGS GEM MT 9.5". 이걸 안 넣으면 그 매물이 미감정 칸에 그대로 남는다.
// ⚠️⚠️ `gem` 뒤의 `mt`·`mint`는 **있을 수도 없을 수도 있다.** 예전엔 `gem\\s*-?\\s*mt`로
//    못 박아 두어 「**PSA Gem 10**」을 통째로 놓쳤다 — 사장님이 짚어 주신 그 매물이
//    정확히 그 꼴이었고(`… Celebrations 15/82 PSA Gem 10`), 그래서 미감정 칸에 남아 있었다.
// ⚠️⚠️ **사이에 끼는 등급말이 생각보다 많다.** 실제 매물 제목 2,174가지를 훑어 넓혔다
//    (2026-08-19) — `PSA EX 5` · `PSA GMT MT 10` · `PSA VG 3` · `CGC MINT+ 9.5` ·
//    `CGC grade = 8.5` · `Ace Grading 10`. 안 넣으면 그만큼이 「확인 안 됨」에 쌓인다.
//    ⚠️ `GMT MT`·`GM`·`PR` 같은 줄임말도 온다. 붙여 쓴 것(`Psa9`)도 있어 회사 뒤의
//       낱말 경계는 **글자만** 막는다(숫자는 붙어도 받는다).
const 사이말 =
  '(?:grade|grading|graded|gold|gem|gmt|gm|pristine|mint|nm|ex|vg|gd|po|fr|pr|mt|\\+|=|:|-|/|\\s)*'
const 등급수 = '(10|[1-9](?:\\.5)?)'
/**
 * **BGS는 Beckett Grading Services의 줄임말이다** — 같은 회사를 두 번 적은 것이다
 * (사장님 지적 2026-09-02). 제목에 「Beckett (BGS) 9」처럼 둘을 나란히 적는 판매자가
 * 있는데, 괄호와 두 번째 회사 이름이 사이말에 없어 **등급을 못 읽고 미감정에 남았다**
 * (실측: 미감정 65,377건 중 11건).
 * ⚠️ **여기서만 지운다 — 회사말·사이말은 안 건드린다.** 사이말에 괄호를 넣으면
 *    「PSA (10)」·「CGC (10)」까지 한꺼번에 달라져 다른 데가 조용히 틀어진다.
 *    한 꼴만 좁게 고치고, 나머지는 사장님이 매물을 보고 정하시기로 했다.
 */
/**
 * **등급이 아닌 것을 미리 지운다**(사장님과 매물을 한 건씩 보며 정함 2026-09-03).
 *
 * 미감정 칸에 남은 72건을 사장님과 함께 훑어 나온 잣대다. 지우지 않으면 정규식이
 * **등급이 아닌 숫자를 등급으로 읽는다.**
 *
 *   ① `CGC 1/1` · `Guinness World Record` — 1/1은 등급이 아니라 **세계에 한 장뿐**이라는
 *      뜻이다. 저쪽(PPT)도 이걸 **CGC 1로 잘못 읽고 있다**(실측 6건) — 저쪽 값을 그대로
 *      믿으면 안 되는 자리다. 기네스 기록 카드가 CGC 1(거의 찢어진 것)일 리 없다.
 *   ② `Low Pop` · `1 OF 3` — 수량 이야기다.
 *   ③ `PSA 2/100`처럼 **번호/세트 크기** — 카드에 찍힌 번호다(거북왕 델타종이 실제로
 *      크리스탈 가디언즈 **2번 / 100장**짜리다). 뒤 숫자가 11 이상이면 등급이 아니다.
 *   ④ `CGC 9 / 2017`처럼 **뒤가 연도**면 앞 숫자가 등급이다(1996~2030).
 *
 * ⚠️ **지우는 것이지 읽는 것이 아니다.** 여기서 지운 자리는 아래 정규식이 아예 안 본다.
 */
const 등급아닌것지우기 = (s: string, 내번호?: string): string =>
  s
    // 1/1 · 기네스 기록 — 등급이 아니라 수량
    .replace(/\b(psa|bgs|cgc|sgc)\s*1\s*\/\s*1\b/gi, '$1')
    // ④ 뒤가 **연도**면 슬래시째 떼어 앞 숫자를 등급으로 읽게 한다(「CGC 9 / 2017」).
    .replace(/\b(psa|bgs|cgc|sgc)(\s*\d{1,2}(?:\.5)?)\s*\/\s*(\d{4})\b/gi, (m, 회사, 값, 해) =>
      Number(해) >= 1996 && Number(해) <= 2030 ? `${회사}${값} ` : m,
    )
    // ③ 「번호/세트크기」 — 뒤가 11 이상이면 카드 번호다
    .replace(/\b(psa|bgs|cgc|sgc)\s*\d{1,2}\s*\/\s*(\d+)\b/gi, (m, 회사, 뒤) =>
      Number(뒤) >= 11 ? String(회사) : m,
    )
    // ⑤ 「PSA #9」의 9가 **그 카드 번호와 같으면** 등급이 아니라 카드 번호다
    //    (사장님 지적 2026-09-03: 리자몽 「NM 7 PSA #1」은 1번 카드의 PSA 7이었다).
    //    지워야 아래 「회사 앞 숫자」가 잡힌다.
    .replace(/\b(psa|bgs|cgc|sgc)\s*#\s*(\d{1,3})\b/gi, (m, 회사, 값) =>
      내번호 && String(Number(값)) === String(Number(내번호)) ? String(회사) : m,
    )

/**
 * **회사가 둘이면 앞에 적힌 것이 실제 슬랩이다** — 「BGS (PSA) 8」·「TAG (PSA) 10」
 * (사장님 판단 2026-09-03: "판매자가 왜 저렇게 쓴 건지는 모르겠는데 bgs 8이라고 보는 게 맞을
 * 것 같아"). 뒤 회사를 지워야 정규식이 앞 회사를 집는다 — 안 지우면 **강철톤 BGS 8이
 * PSA 8로** 들어갔다(실측 7건).
 */
const 뒤회사지우기 = (s: string): string =>
  s.replace(/\b(psa|bgs|cgc|sgc|beckett|ace|tag|csg|pgs)\b\s*[([]\s*(?:psa|bgs|cgc|sgc)\s*[)\]]/gi, '$1 ')

const 겹친회사이름지우기 = (s: string): string =>
  s.replace(/\bbeckett\s*\(\s*bgs\s*\)/gi, 'BGS').replace(/\bbgs\s*\(\s*beckett\s*\)/gi, 'BGS')

/**
 * **반칸 등급에 낱말이 붙어 있으면 띄운다** — 「PSA 8.5NM Mint+」(2026-09-02 실측).
 *
 * 붙여 쓰면 `8.5` 뒤에 낱말 경계가 없어 정규식이 **`.5`를 버리고 `8`로 읽는다.**
 * 8.5짜리 낙찰이 8 칸에 담기면 값이 낮은 쪽으로 섞인다.
 * ⚠️ **`x.5` 뒤에 글자가 올 때만** 띄운다. 「10」 같은 온등급까지 풀면 「PSA 10th
 *    Anniversary」의 10을 등급으로 읽는다 — 그건 등급이 아니라 기념판 이름이다.
 */
const 반칸띄우기 = (s: string): string => s.replace(/(\d\.5)([A-Za-z])/g, '$1 $2')

/**
 * **이베이가 제목 끝에 붙여 보내는 꼬리말을 뗀다** — 「… PSA 8Opens in a new window or tab」.
 *
 * 「8Opens」처럼 **등급 숫자에 글자가 딱 붙어** 낱말 경계가 사라진다. 그러면 「회사 뒤 숫자」
 * 규칙이 통째로 실패하고, 그 뒤에 있는 **엉뚱한 숫자**를 등급으로 읽는다(실측 2026-09-03:
 * 「Arceus AR3 PSA 8Opens」를 **PSA 3**으로, 「#SH1 PSA 9Opens」를 **PSA 1**로 읽었다).
 * ⚠️ 앞머리의 「Sold  Nov 30, 2025」도 같이 뗀다 — 연도가 등급 자리에 끼어든다.
 */
const 꼬리말떼기 = (s: string): string =>
  s
    .replace(/Opens?\s+in\s+a\s+new\s+window[^]*$/i, '')
    .replace(/(\d)Opens?\b[^]*$/i, '$1')
    .replace(/^\s*Sold\s+[A-Z][a-z]{2}\s+\d{1,2},\s*\d{4}/i, ' ')

/**
 * @param 내번호 그 카드의 도감 번호(알면 넘긴다). 「PSA #9」의 9가 이 번호와 같으면
 *   등급이 아니라 카드 번호로 보고 건너뛴다 — 안 넘겨도 나머지 규칙은 그대로 돈다.
 */
export const 제목등급칸 = (t: string, 내번호?: string): string => {
  const s = 등급아닌것지우기(뒤회사지우기(반칸띄우기(꼬리말떼기(겹친회사이름지우기(String(t ?? ''))))), 내번호)
  // ① 흔한 꼴: 「회사 … 숫자」
  let m = s.match(new RegExp(`\\b(${회사말})(?![a-z])${사이말}${등급수}\\b(?!\\d|\\s*\\/\\s*\\d)`, 'i'))
  // ② 뒤집힌 꼴: 「PRISTINE 10 CGC」·「Ace Grading 10 … Not PSA」처럼 숫자가 앞에 온다.
  if (!m) {
    const r = s.match(new RegExp(`\\b(?:pristine|gem\\s*mt|mint)\\s*${등급수}\\b[^\\n]{0,24}?\\b(${회사말})\\b`, 'i'))
    if (r) m = [r[0], r[2], r[1]] as unknown as RegExpMatchArray
  }
  // ③ **회사 앞 숫자** — 「NM 7 PSA」·「Latios-Holo 9 PSA」. 위에서 카드 번호를 지운 뒤라야
  //    여기까지 온다(안 지우면 「PSA #1」의 1이 먼저 잡힌다).
  //    ⚠️⚠️⚠️ **앞에 상태말(NM·Mint·Graded…)이 있을 때만 본다.** 그냥 「숫자 + 회사」로
  //       열어 두면 카드 번호가 통째로 걸린다 — 실측(2026-09-03)에서 「#SH1 PSA 9」를
  //       **PSA 1**로, 「POP 2 CGC PERFECT 10」을 **CGC 2**로, 「1 of 1 CGC」를 **CGC 1**로
  //       읽었다. 상태말을 앞세우면 「NM 7 PSA」·「Graded 8 PSA」만 남는다.
  if (!m) {
    const r2 = s.match(
      new RegExp(`\\b(?:nm|mt|mint|gem|graded?|grade|ex|vg|pristine|perfect|m\\+|nm-mt|nm\\+)[^a-z0-9]{0,6}${등급수}\\s+(${회사말})(?![a-z])`, 'i'),
    )
    if (r2) m = [r2[0], r2[2], r2[1]] as unknown as RegExpMatchArray
  }
  if (!m) {
    // 회사 이름이 뒤에 오는 꼴: "GEM MT 10 ... PSA" 는 위에서 잡히고, 여긴 "Z gold 10" 같은 것.
    const z = s.match(new RegExp(`\\b(?:pokemon-?)?z\\s*gold\\s*${등급수}\\b(?!\\d)`, 'i'))
    if (z) return '기타' // 「Z gold 10」류 — 작은 회사라 기타로 모은다
    return ''
  }
  const 회사 = m[1].toLowerCase() === 'beckett' ? 'bgs' : m[1].toLowerCase()
  // ⚠️⚠️ **큰 회사 넷만 등급별로 칸을 나눈다**(사장님 지시 2026-08-19).
  //    나머지(PCG·PGS·MPG·TAG·ACE·SGC·CSG…)는 **「기타 감정 회사」 한 칸**으로 모은다 —
  //    작은 회사 이름으로 칸을 다 세우면 화면이 칸으로 뒤덮이고, 방문자에게 필요한 것은
  //    「이건 큰 회사 감정이 아니다」 하나다. **낱개 줄에는 회사 이름이 그대로 보인다.**
  //    ⚠️ `brg`는 사장님이 적어 주셨는데 **우리 자료에는 아직 한 번도 안 왔다**
  //       (덤프의 회사는 psa·cgc·tag·bgs·ace·sgc뿐 · 실제 매물 제목 3,190건에도 0회).
  //       올 때를 위해 넣어 둔다.
  if (!큰회사.has(회사)) return '기타'
  const 칸 = `${회사}${m[2].replace('.', '_')}`
  // ⚠️⚠️ **10 위의 등급을 따로 세운다**(사장님 지시 2026-08-19).
  //    CGC의 「Pristine 10」은 CGC 10보다 **위 등급**이고 값도 다르다(같은 카드에서
  //    93.5만원 ↔ 54.3만원). BGS의 「Black Label 10」도 마찬가지다.
  //    **값 차이를 재서 정할 일이 아니라, 애초에 다른 등급이라 갈라야 하는 것이다.**
  //    ⚠️ 「PRI」로 줄여 쓰는 매물이 많다(「CGC 10 PRI …」). 낱말 경계로 받는다.
  //    ⚠️ 저쪽(PPT) 덤프는 제목이 없어 못 가른다 — 그건 `cgc10`으로 그대로 온다.
  //       두 길이 섞여도 안 깨진다(칸 이름이 다를 뿐이라 각자 제 줄로 선다).
  // ⚠️⚠️⚠️ **BGS와 CGC는 「10점」의 짜임이 반대다**(2026-08-27 슬랩 실물로 확인).
  //
  //  BGS — 10점의 **이름이 하나뿐**이다. 라벨에 늘 「PRISTINE 10」이 찍힌다.
  //        검은 라벨 = 부분점수 넷이 전부 10 · 금색 라벨 = 9.5가 하나 섞임.
  //        ⚠️ 그러니 **「Pristine」과 「10」은 같은 말이다** — 따로 세면 안 된다.
  //           실물 확인: 「BGS 10」으로 검색해 나온 슬랩들이 죄다 「10 PRISTINE · 센터링 9.5」였다.
  //           갈라 놨더니 $500(프리스틴 적힘) ↔ $325(안 적힘)로 갈렸는데, 그건 등급 차이가
  //           아니라 **제목을 자세히 쓰는 판매자가 값도 더 부르는 것**이다.
  //        → 가르는 것은 **「Black Label」이라 적혔는가** 하나뿐이다(중앙값 $3,800 ↔ $400대).
  //
  //  CGC — 10점에 **이름이 둘**이고 라벨에 다르게 찍힌다.
  //        「PRISTINE 10」(검은 라벨) > 「GEM MINT 10」(흰 라벨). 값도 $60 ↔ $42.
  //        ⚠️ 옛 「Perfect 10」은 **2023년 7월에 없어진 등급**이다(CGC·CSG 합병 때
  //           「10점이 셋이면 너무 많다」고 정리). 옛 슬랩만 남아 있고 물량도 적은데,
  //           제목으로 잡으려 하면 **세트 이름 「Perfect Order」를 잘못 문다**(실측으로
  //           걸린 556건 상당수가 그것이었다). 잡지 않고 `cgcp10`에 둔다.
  //
  // ⚠️ 「PRI」로 줄여 쓰는 매물이 많다(「CGC 10 PRI …」). 낱말 경계로 받는다.
  if (칸 === 'cgc10' && /\bpri(?:stine)?\b/i.test(s)) return 'cgcp10'
  if (칸 === 'bgs10' && /\bblack\s*label\b/i.test(s)) return 'bgsbl10'
  return 칸
}

/**
 * 제목에서 「회사 + 등급」을 사람 눈에 보이는 꼴로 — "ACG 9" · "Z GOLD 10". 못 찾으면 ''.
 * 「기타 감정 회사」 칸의 낱개 줄에 붙는다(사장님 지시 2026-08-20) — 그 칸은 회사도 등급도
 * 섞여 있어서, 줄마다 어느 회사의 몇 점인지 밝혀야 낱개가 뜻을 가진다.
 * ⚠️ 위 `제목등급칸`과 같은 정규식(사이말·등급수)을 쓴다 — 따로 두면 칸과 표기가 어긋난다.
 */
export const 제목회사표기 = (t: string): string => {
  const s = String(t ?? '')
  let m = s.match(new RegExp(`\\b(${회사말})(?![a-z])${사이말}${등급수}\\b(?!\\d|\\s*\\/\\s*\\d)`, 'i'))
  if (!m) {
    const r = s.match(new RegExp(`\\b(?:pristine|gem\\s*mt|mint)\\s*${등급수}\\b[^\\n]{0,24}?\\b(${회사말})\\b`, 'i'))
    if (r) m = [r[0], r[2], r[1]] as unknown as RegExpMatchArray
  }
  if (m) return `${m[1].toUpperCase()} ${m[2]}`
  const z = s.match(new RegExp(`\\b(?:pokemon-?)?z\\s*gold\\s*${등급수}\\b(?!\\d)`, 'i'))
  if (z) return `Z GOLD ${z[1]}`
  return ''
}

/**
 * **제목에 감정 회사 이름이 하나라도 나오나.**
 *
 * ⚠️ 「등급을 못 읽었다」와 「애초에 감정 안 한 카드다」는 **전혀 다른 말**이다.
 *    회사 이름이 아예 없으면 그건 **미감정 싱글카드**라 값을 보여 줘야 한다.
 *    회사 이름은 있는데 등급을 못 읽은 것만 「확인 안 됨」으로 둔다.
 *    (사장님이 「4개는 그냥 미감정 같고 1개는 PCG 같은데」라고 5초 만에 가르신 그 구분이다.)
 */
export const 제목에감정사있나 = (t: string): boolean =>
  new RegExp(`\\b(?:${회사말}|z\\s*gold)\\b`, 'i').test(String(t ?? ''))
/** 그 칸의 회사 이름(ungraded면 빈값). "cgc8_5" → "cgc" */
/**
 * **여러 장을 한꺼번에 판 매물인가.** 한 장 값이 아니라서 시세에 넣으면 안 된다.
 *
 * 원본 낙찰 24,373건에 걸어 보니 **1건**뿐이었다(2026-08-08) — 아주 드물다. 대신
 * 걸리면 값이 통째로 틀어진다("Crown Zenith Mewtwo VSTAR … Set of 3" $153.5가 psa9에).
 * ⚠️ **헛발이 없게 좁게 잡았다.** "PSA 6 Card"의 "6 Card" 같은 것을 세면 멀쩡한 낙찰
 *    수천 건이 걸린다(처음에 그랬다). "lot of 3"처럼 **수량을 말하는 꼴**만 본다.
 * ⚠️ 목록에서 지우지는 않는다. 평균·중앙값·그래프에서만 뺀다 — 사람이 보고 판단하게.
 */
export const 묶음인가 = (t: string | undefined): boolean => {
  const s = String(t ?? '')
  if (/\b(?:lot|set|bundle|collection|joblot)\s*of\s*\d+|\bjob\s*lot\b|\b\d+\s*card\s*lot\b|\bbundle\s*of\b/i.test(s))
    return true
  // ⚠️ **카드 번호가 둘 이상 적힌 제목은 여러 장을 같이 판 것이다.**
  //    "Umbreon VMAX 245/184 & Umbreon 244/184" · "Latias ex 239/191 and Latios 203/191"
  //    한 장 값이 아닌데 한 장 값으로 들어가면 시세가 부풀려진다. 실제로 방문자가 자주
  //    찾는 피카츄 227/S-P에 "Pikachu 227/S-P + Cramorant 226/S-P" $3,746이 있었다.
  //    낙찰 17,716건에 걸어 34건(0.19%)이 나왔고 **전부 진짜 묶음**이었다(2026-08-08).
  //    ⚠️ 같은 번호를 두 꼴로 적은 것("232/091"·"232/91")은 번호맞추기가 하나로 모으므로
  //       여기 안 걸린다.
  //    ⚠️ 프로모는 분모가 글자다("227/S-P"·"SWSH123"). 그건 카드 번호로는 안 쓰지만
  //       **묶음을 가릴 때는 센다** — "Pikachu 227/S-P Cramorant 226/S-P"가 그렇다.
  const 번호꼴 = new Set(제목번호들(s))
  //    ⚠️ 글자 분모는 **가운뎃줄이 있는 꼴만** 본다(S-P · SM-P · XY-P). 안 그러면
  //       "Base Set 2 / Base II"의 "2 / Base"를 번호로 읽는다(2026-08-08에 겪음).
  for (const m of s.matchAll(/(^|[^\w/])(\d{1,4})\s*\/\s*([A-Za-z]{1,3}-[A-Za-z]{1,2})\b/g))
    번호꼴.add(`${Number(m[2])}/${m[3].toUpperCase()}`)
  return 번호꼴.size >= 2
}

export const 칸회사 = (칸: string): string => (칸 === 'ungraded' ? '' : (칸.match(/^[a-z]+/i)?.[0] ?? '').toLowerCase())

/**
 * 제목의 번호에서 **앞에 붙은 세트 표시를 떼어** 같은 카드끼리 모이게 한다.
 *
 * 파는 사람이 "07 03/09"를 붙여 "0703/09"로 적는다. 그대로 세면 "0703/09"와 "03/09"가
 * 딴 카드가 되어, 같은 카드가 두 갈래로 쪼개진다.
 * ⚠️ **앞자리를 무턱대고 떼면 안 된다.**
 *    · "104/128"의 104는 진짜 세 자리 번호다.
 *    · "252/184"처럼 **앞 번호가 총 장수보다 큰 것도 진짜다**(시크릿 레어).
 *      그래서 "앞이 뒤보다 크면 뗀다"로는 안 된다 — 실제로 252/184가 52/184로 깨졌다.
 *    붙여 적은 것은 자릿수가 딱 **뒤 자릿수 + 2**가 된다("07"+"03"/"09" → 4자리 = 2+2).
 *    그때만 앞 두 자리를 뗀다.
 */
export const 번호맞추기 = (앞: string, 뒤: string): string => {
  const a = 앞.length === 뒤.length + 2 ? 앞.slice(2) : 앞
  return `${Number(a)}/${Number(뒤)}`
}
export const 제목번호들 = (t: string): string[] => {
  const out = new Set<string>()
  // ⚠️ **소수점 뒤는 번호가 아니다.** BGS는 부점수를 "9.5/9.5/10/9.5"로 적는데,
  //    앞을 안 막으면 ".5/9"에서 **5/9를 카드 번호로 읽는다**(2026-08-08에 잡음).
  //    카드 번호가 마침표 바로 뒤에 오는 일은 없다.
  for (const m of String(t).matchAll(/(^|[^\d/.])(\d{1,4})\s*\/\s*(\d{1,3})(?![\d/])/g)) {
    if (등급앞말.test(String(t).slice(0, (m.index ?? 0) + m[1].length))) continue
    const n = 번호맞추기(m[2], m[3])
    // ⚠️ **"1/1"은 카드 번호가 아니라 자랑말이다** — 감정 케이스 매물에서 "POP 1"·
    //    "ARS10+ BLACK 1/1"처럼 "세상에 한 장"을 뜻한다. 포켓몬 카드에 1/1 번호는
    //    사실상 없다. 이걸 번호로 읽으면 멀쩡한 낙찰이 딴 카드로 걸린다(2026-08-08).
    if (n === '1/1') continue
    out.add(n)
  }
  return [...out]
}

/**
 * **분모 없이 "#38"처럼 적은 카드 번호.** 옛 카드 매물에 흔하다.
 *
 * ⚠️ **이것만으로 딴 카드를 가리면 안 된다.** 예전에 "번호 앞자리"로 견주다가 표본에서
 *    12건을 잘못 잘랐다 — 뮤는 도감번호 151을 제목에 쓰고, LEGEND는 상·하 두 장이라
 *    "#89 and #90"이라 적는다. 그래서 서버는 **연도까지 함께 어긋날 때만** 뺀다.
 * ⚠️ 반드시 `#`가 앞에 붙은 것만 본다(등급 숫자·인증번호와 섞이지 않게). 세 자리까지만
 *    보고, 뒤에 "/"가 오면 그건 분모 있는 번호라 여기서 안 본다.
 */
export const 맨번호들 = (t: string): string[] => {
  const out = new Set<string>()
  for (const m of String(t).matchAll(/(^|[^\w/])#\s*(\d{1,3})\b(?!\s*\/)/g)) out.add(String(Number(m[2])))
  return [...out]
}
