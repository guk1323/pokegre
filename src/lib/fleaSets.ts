// 플리마켓에서 다루는 **판**과 **확장팩**. 화면과 서버가 이 한 벌만 쓴다.
//
// ⚠️⚠️ 왜 좁히나 — 우리 목표는 **여기서 한글판 시세를 만드는 것**이다(사장님 2026-08-11).
//    아무 카드나 올리게 두면 3만 장에 매물이 한두 개씩 흩어져, 같은 카드가 여럿 모이는
//    일이 없어 시세가 영영 안 생긴다. 최근 확장팩으로 모아야 파는 사람과 사는 사람이
//    같은 카드에서 만난다.
//
// ⚠️ **영문판(na)은 안 받는다.** 한국에서 오가는 것은 한글판과 일본판이다.
//
// 늘리려면 여기 한 줄만 더하면 된다 — 화면·서버가 같이 따라온다.

export const FLEA_EDITION_LABEL = { jp: '일본판', kr: '한글판' } as const;
export type FleaEdition = keyof typeof FLEA_EDITION_LABEL;

/**
 * 올릴 수 있는 확장팩. 최근 6개다.
 *
 * ⚠️ M6(스톰에메랄다)는 **일부러 뺐다.** 일본판만 나오고 **한글판이 아직 발매 전**이라
 *    한글 그림도 이름도 없다. 한국 발매는 일본보다 두세 달 늦는다 — 나오면 여기에 넣는다.
 * ⚠️ 세트 코드는 한국·일본이 같다(M5·M4…). 한국은 일본판 세트를 그대로 내므로,
 *    한글판이라고 따로 카탈로그를 두지 않고 일본판 세트에 한글 그림·이름을 붙여 쓴다.
 */
export const FLEA_SET_SLUGS = [
  'ja-M5', // 어비스아이   2026-05-22
  'ja-M4', // 닌자스피너   2026-03-13
  'ja-M3', // 니힐제로     2026-01-23
  'ja-M2', // 인페르노X    2025-09-26
  'ja-M1L', // 메가브레이브  2025-08-01
  'ja-M1S', // 메가심포니아  2025-08-01
] as const;

export const FLEA_SET_SET: ReadonlySet<string> = new Set(FLEA_SET_SLUGS);
